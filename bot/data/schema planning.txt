CREATE TABLE "pkmn" (
    `id`    INTEGER NOT NULL,
    `natdex_no`    INTEGER NOT NULL,
    `galardex_no`   INTEGER,
    `name`  TEXT,
    `category` TEXT,
    `egg_group` TEXT,
    `type_1`  TEXT,
    `type_2`  TEXT,
    `stage` INTEGER,
    `evo_method` TEXT,
    `tier`  INTEGER,
    `hp`    INTEGER,
    `atk`   INTEGER,
    `def`   INTEGER,
    `spatk`   INTEGER,
    `spdef`   INTEGER,
    `speed` INTEGER,
    `abilities` TEXT,
    `evs`   TEXT,
    `catch` INTEGER,
    `gender`    REAL,
    `mass_kg`   REAL,
    `image` TEXT,
    `shiny` TEXT,
    `gif`   TEXT,
    `sprite`    TEXT,
    `emoji` TEXT,
    `pokedex_entry` TEXT,
    `notes` TEXT,
    PRIMARY KEY(`id`)
)

to add:

ev data (null for now)
location (sw/sh)
evolution method
hatch time???